It is kind of on demand transactions and on demand balance, will it work with retry? [[Retry when bank throws an error]]
Raw data will be provided by this service. 
With this connect is [[bank synchronization service]] that will also handle [[duplicates]] (either filter them our or ignore them)
[[bank synchronization service]] will also have a tool that will allow client to put his own mapping file for bank provided data. 